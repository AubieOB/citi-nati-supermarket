import React from 'react';
import AdminStocks from '../admin/AdminStocks';

/**
 * Wrapper for AdminStocks to use in window system
 */
const WindowedStocks = () => {
  return <AdminStocks />;
};

export default WindowedStocks;
