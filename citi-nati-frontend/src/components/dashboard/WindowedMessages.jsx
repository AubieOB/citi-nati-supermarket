import React from 'react';
import AdminInbox from '../admin/AdminInbox';

/**
 * Wrapper for AdminInbox (Help Center/Tickets) to use in window system
 */
const WindowedMessages = () => {
  return <AdminInbox />;
};

export default WindowedMessages;
