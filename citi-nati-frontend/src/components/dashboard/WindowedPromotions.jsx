import React from 'react';
import AdminPromotions from '../admin/AdminPromotions';

/**
 * Wrapper for AdminPromotions to use in window system
 */
const WindowedPromotions = () => {
  return <AdminPromotions />;
};

export default WindowedPromotions;
